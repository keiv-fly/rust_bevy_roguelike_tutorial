[package]
name = "bevy_roguelike_tutorial_ch03"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = { version = "0.7.0"}

[dev-dependencies]
bevy = { version = "0.7.0", features = ["dynamic"] }
